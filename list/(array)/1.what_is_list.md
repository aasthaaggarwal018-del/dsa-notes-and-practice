in python list allow to store multiple values in one variable
for eg age of the students of a class

list are similar to array in other languages

properties:

ordered(the way data entred)
mutable(item can be changed)
allow duplicate(item can be duplicate)

oprations in list :

[click this link](list_oprations.py)