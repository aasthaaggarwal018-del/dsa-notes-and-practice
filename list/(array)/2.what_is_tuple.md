tuple is a collention similary to list the primary differnce is
that we can not modify a tuple after create

properties:

immutable(cannot chnage)
orderd
allow duplicate

operations:

[click here : ](tuple_operations.py)